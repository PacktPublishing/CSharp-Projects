{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": ["build", "ModelContextProtocol/ModelContextProtocol.sln"],
            "problemMatcher": "$msCompile",
            "group": {"kind": "build", "isDefault": true}
        }
    ]
}
